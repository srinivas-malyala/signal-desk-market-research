resources:
  volumes:
    signal_desk_raw:
      name: ${var.raw_volume}
      catalog_name: ${var.catalog}
      schema_name: ${var.schema}
      volume_type: MANAGED
      comment: Immutable raw landing area for Massive and SEC responses
