resources:
  pipelines:
    research_pipeline:
      name: signal-desk-research-pipeline-${bundle.target}
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      development: ${var.pipeline_development}
      continuous: false
      photon: true
      channel: current
      root_path: ../pipelines
      libraries:
        - glob:
            include: ../pipelines/_article_pipeline_common.py
        - glob:
            include: ../pipelines/_sec_pipeline_common.py
        - glob:
            include: ../pipelines/document_text.py
        - glob:
            include: ../pipelines/bronze_research_articles.py
        - glob:
            include: ../pipelines/bronze_sec_company_facts.py
        - glob:
            include: ../pipelines/bronze_sec_filing_documents.py
        - glob:
            include: ../pipelines/bronze_sec_manifests.py
        - glob:
            include: ../pipelines/bronze_sec_submissions.py
        - glob:
            include: ../pipelines/silver_article_tickers.py
        - glob:
            include: ../pipelines/silver_research_articles.py
        - glob:
            include: ../pipelines/silver_research_chunks.py
        - glob:
            include: ../pipelines/silver_sec_companies.py
        - glob:
            include: ../pipelines/silver_sec_facts.py
        - glob:
            include: ../pipelines/silver_sec_filings.py
        - glob:
            include: ../pipelines/sec_structured_reconciliation.py
        - glob:
            include: ../pipelines/research_chunk_reconciliation.py
        - glob:
            include: ../pipelines/gold_industry_peer_comparison.py
        - glob:
            include: ../pipelines/gold_research_catalog.py
      configuration:
        signal_desk.raw_volume_path: /Volumes/${var.catalog}/${var.schema}/${var.raw_volume}
      permissions:
        - level: CAN_VIEW
          group_name: users
